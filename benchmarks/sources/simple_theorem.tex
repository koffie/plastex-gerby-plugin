%# tags
\documentclass{article}

% No \usepackage{amsthm} -- gerby-project/plastex has \newtheorem and \proof
% built in. This file is used only for benchmark generation via
% benchmarks/generate_benchmarks.py using the gerby venv (.venv-gerby).
% For the upstream plugin tests see tests/gerby_rendering/sources/simple_theorem.tex,
% which adds \usepackage{amsthm} as required by upstream plasTeX.

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}

\begin{document}

\section{Introduction}
\label{sec:intro}

\begin{theorem}
\label{thm:primes}
There are infinitely many prime numbers.
\end{theorem}

\begin{proof}
Suppose for contradiction that there are only finitely many primes
$p_1, \ldots, p_n$. Let $N = p_1 \cdots p_n + 1$. Then $N$ is not
divisible by any $p_i$, a contradiction.
\end{proof}

\begin{lemma}
\label{lem:euclid}
Every integer greater than $1$ has a prime divisor.
\end{lemma}

\begin{proof}
By induction on the integer.
\end{proof}

\end{document}
